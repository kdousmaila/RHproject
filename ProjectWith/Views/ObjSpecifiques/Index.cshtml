@model IEnumerable<ProjectWith.Models.ObjSpecifique>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ObjSpecifique1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Synthese)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EntretienAnnuel.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ObjSpecifique1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Synthese)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EntretienAnnuel.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
