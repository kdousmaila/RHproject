
@model ProjectWith.Models.EvaluatinGenN_1



<tr>
    <td>@Model.performance</td>
    <td>@Model.commentaire</td>
    <td>@Model.tendance</td>
    <td>

        <a href="#" class="delete-link" data-id="@Model.id">Delete</a>



    </td>
</tr>


@Html.AntiForgeryToken()


@*<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    $(document).ready(function () {
        $('#EvalGenTable').on('click', '.delete-link', function (e) {
            e.preventDefault();
            var id = $(this).data('id');
            var confirmation = confirm('Are you sure you want to delete this item?');
            if (confirmation) {
                $('.alert').hide();
                deleteItem(id);
            }
        });

        function deleteItem(id) {
            $.ajax({
                url: '@Url.Action("DeleteConfirmed", "EvaluatinGenN_1")',
                type: 'POST',
                data: {
                    id: id,
                    __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()
                },
                success: function (result) {
                    $('a[data-id="' + id + '"]').closest('tr').remove();
                },
                error: function (xhr, status, error) {
                    alert('An error occurred while trying to delete the item.');
                }
            });
        }
    });
</script>*@